<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".on.board.Board4Fragment">

    <TextView
        android:id="@+id/tv_start"
        android:text="Начать работу"
        android:textSize="16sp"
        android:textColor="#B1B1B1"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/tv_Board4Fragment"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_marginTop="40dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/lottie_board4"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:lottie_autoPlay="true"
        app:lottie_repeatCount="1000"
        android:layout_marginBottom="170dp"
        android:elevation="5dp"
        app:lottie_rawRes="@raw/board3"
        android:layout_width="320dp"
        android:layout_height="300dp"/>

    <TextView
        android:id="@+id/tv_Board4Fragment"
        android:text="Куча функций и интересных фишек"
        android:textSize="19sp"
        android:textColor="@color/white"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/lottie_board4"
        android:layout_marginTop="40dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

</androidx.constraintlayout.widget.ConstraintLayout>